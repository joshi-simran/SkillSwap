# SkillSwap Frontend (Vite + React)

## Setup
1. `npm install`
2. Copy `.env.example` to `.env` and set `VITE_API_URL` if different.
3. `npm run dev`

Login with seeded backend user: `alex@example.com` / `password123` (after running backend seed).
