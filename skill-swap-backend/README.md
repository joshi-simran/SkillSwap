# SkillSwap Backend

## Setup
1. `npm install`
2. Copy `.env.example` to `.env` and fill values.
3. (Optional) `npm run seed` to create sample users/skills.
4. `npm run dev`

By default allows CORS from `http://localhost:5173`.
